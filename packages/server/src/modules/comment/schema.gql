type Comment {
  id: ID!
  content: String
  imageURL: String
  status: Status!
  owner: Owner!
  createdAt: DateTime!
  updatedAt: DateTime!
}

extend type Mutation {
  createComment(statusId: ID!, content: String!, imageURL: String): Comment!
  deleteComment(commentId: ID!): Comment
}
