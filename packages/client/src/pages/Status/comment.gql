mutation createComment($statusId: ID!, $content: String!, $imageUrl: URL) {
  createComment(statusId: $statusId, content: $content, imageUrl: $imageUrl) {
    id
  }
}

mutation deleteComment($commentId: ID!) {
  deleteComment(commentId: $commentId) {
    id
  }
}
