query getStatus {
  statuses {
    id
    content
    imageURL
    updatedAt
  }
}

mutation createStatus($content: String!, $imageURL: String) {
  createStatus(content: $content, imageURL: $imageURL) {
    id
  }
}
